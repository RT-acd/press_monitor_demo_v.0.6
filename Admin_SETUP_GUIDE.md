# PRESS MONITOR セットアップガイド（v2：マスタ管理・ファイル分割対応）

## 今回のアップデート内容

- **管理者専用ページ（admin.html）を追加**：品番・作業員マスタをExcelファイルからアップロードするだけで、クラウド上のマスタが更新される仕組みにしました
- 画面（HTML）とロジック（JS）をファイル分割し、保守性を向上
- 品番／SPM／作業員を「マスタデータ」としてクラウド（Googleスプレッドシート）で一元管理する方式に変更
- ボタンの不具合（製造開始後に稼働中ボタンが反応しない）を修正

---

## ファイル構成（今回の追加分）

```
admin.html … 管理者専用：マスタアップロード画面
admin.js   … 上記の処理ロジック（Excel解析・列選択・アップロード）
```

これらは他のファイル（index.html / app.js など）とは独立して動作します。**管理者専用ページなので、現場のiPadのホーム画面には追加しないでください。** URLも現場の方には共有せず、管理者の方だけが把握する運用にしてください（パスワード等の保護はかけていないため）。

## 管理者ページの使い方

1. 管理者PCのブラウザで `admin.html` のURL（例：`https://ユーザー名.github.io/リポジトリ名/admin.html`）を開く
2. 上部の「Web App URL」に、iPadアプリの設定画面と同じGAS URLを入力（一度入力すればそのブラウザに記憶されます）
3. 「① 品番・SPMマスタの更新」で、Excelファイル（.xlsx）を選択
4. シート・品番の列・SPMの列を確認（自動推測されますが、違っていれば選び直してください）
5. プレビューで内容を確認し、「このマスタをクラウドへ反映する」をクリック
6. 「② 作業員マスタの更新」も同様に、別のExcelファイルをアップロード

反映すると、クラウド上の対象マスタは**アップロードした内容で完全に置き換わります**（差分追記ではありません）。各iPadは自動的に、または設定画面の「マスタを再取得」で最新内容を取得します。


## 手順1：ファイル構成の確認

今回お渡しするファイルは以下の6つです。**すべて同じフォルダ階層（サブフォルダなし）**にアップロードしてください。

```
index.html      … 画面の構造
style.css       … デザイン
config.js       … 設定値
cloud.js        … クラウド通信
app.js          … メインロジック
admin.html      … 管理者専用：マスタアップロード画面
admin.js        … 管理者ページのロジック
Code.gs         … バックエンド（Apps Script側に貼り付け）
```

`index.html` が `style.css` `config.js` `cloud.js` `app.js` を同じ場所から読み込む作りになっているため、この6点は必ずセットで、同一フォルダに置いてください（1ファイルだけ差し替える、という使い方も可能です）。

## 手順2：GitHubへのアップロード（更新）

既存のリポジトリ（前回作成したもの）を使う場合：

1. リポジトリ画面 → **Add file** → **Upload files**
2. `index.html` `style.css` `config.js` `cloud.js` `app.js` の5ファイルをまとめてドラッグ＆ドロップ
3. 既存の `index.html` は上書き確認が出ますので「はい」で進める
4. **Commit changes** で保存 → 1分程度でGitHub Pagesに反映されます

## 手順3：Google Apps Script（Code.gs）の更新

1. 今お使いのGoogleスプレッドシートを開く → **拡張機能 > Apps Script**
2. 中身を全て選択して削除し、新しい `Code.gs` の内容を貼り付けて保存
3. **デプロイ > デプロイを管理** → 鉛筆アイコン（編集）→ **バージョン: 新しいバージョン** を選択 → **デプロイ**
   （※ここが重要です。新規デプロイではなく「既存のデプロイを更新」を選ばないと、アプリ側のURLが変わってしまいます）

## 手順4：マスタデータシートの入力

再デプロイ後、スプレッドシートを開くと自動的に以下の2つのシートタブが追加されます（アプリを一度動かす、または「マスタを再取得」を押すと作成されます。手動で先に作っておいても構いません）。

### 「品番マスタ」シート
| 品番 | SPM |
|---|---|
| SP-200 | 15 |
| SP-210 | 18 |
| DIE-A100 | 12 |

- A列：品番、B列：SPM（1分あたりの生産数）
- 1行目は見出し行として扱われるため、データは2行目から入力してください
- 行の追加・削除・並び替え・値の修正は全て自由に行えます（Excel感覚でOKです）

### 「作業員マスタ」シート
| 作業員名 |
|---|
| 山田 |
| 佐藤 |
| 鈴木 |

- A列に作業員名を1人1行で入力（人数の上限はありません）
- 退職・異動などがあれば行を削除するだけで、iPad側の選択肢からも消えます

## 手順5：iPad側でマスタを反映

1. iPadでアプリを開く（一度リロードしてください）
2. 右上の歯車アイコン →「マスタを再取得」ボタンをタップ
3. 「マスタデータを更新しました」と表示されればOK
4. モニター画面に戻ると、品番の選択肢と作業員チップが更新されています

※ アプリは起動時と10分ごとに自動でもマスタを再取得します。至急反映したい場合のみ手動ボタンをご利用ください。
※ 電波が悪い場所でも、最後に取得できたマスタデータがiPad内に保存されているため、そのまま使用を継続できます。

---

## 今後の改修について

このアプリは今後AIに改修を依頼する前提で、役割ごとにファイルを分けています。改修を依頼する際の目安です。

| 変更したい内容 | 主に触るファイル |
|---|---|
| 画面のレイアウト・デザイン | `index.html` `style.css` |
| タイマーや稼働率の計算ロジック、ボタンの動き | `app.js` |
| クラウドとの通信内容（同期・マスタ取得・メール送信） | `cloud.js` |
| 初期値・定数（目標数のデフォルト値など） | `config.js` |
| スプレッドシートへの保存形式、メール本文・Excel添付の内容 | `Code.gs` |

「〇〇の動きがおかしい／〇〇の機能を追加したい」という形で伝えていただければ問題ありません。ファイル名を意識する必要はなく、AI側で該当箇所を特定して修正します。

---

## よくある質問（追加分）

**Q. 品番マスタにない品番を一時的に使いたい**
A. 現在の仕様では品番マスタに登録された品番のみ選択できます。臨時の品番が必要な場合は、まず「品番マスタ」シートに1行追加してから、iPad側で「マスタを再取得」してください。

**Q. SPMの値をその場で調整したい**
A. 品番選択でSPMは自動入力されますが、その後も画面上のSPM欄は手動で上書き可能です（一時的な調整用）。恒久的に値を変えたい場合はマスタシート側を更新してください。

**Q. 設備名リストもクラウド管理にできますか？**
A. 現状は端末ごとの設定のままです（工場によって設備の使い方が異なるため）。品番・作業員と同様にクラウド化することも可能ですので、ご要望があればお知らせください。
